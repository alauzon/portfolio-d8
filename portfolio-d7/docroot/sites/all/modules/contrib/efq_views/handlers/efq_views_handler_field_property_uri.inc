<?php
// $Id$

/**
 * Field handler for URL/URI properties.
 */
class efq_views_handler_field_property_uri extends entity_views_handler_field_uri {
  /**
   * Override the parent's query method, since it doesn't need to do anything.
   */
  function query() {}
}
